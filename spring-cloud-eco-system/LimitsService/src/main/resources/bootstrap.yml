spring:
  application:
    name: limits-service
  cloud:
    config:
      uri: http://localhost:8080
  profiles:
    active: dev

server:
  port: 9000

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka